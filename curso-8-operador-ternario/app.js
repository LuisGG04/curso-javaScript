// Operador ternario

// (condicion) ? true : false

// (condicion) ?
// (primera sentencia,
// segunda sentencia)
// :
// (primera sentencia,
// segunda sentencia);

let num = 4;

(num % 2 == 0) ? console.log(`${num} es un número par`) : console.log(`${num} es un número impar`);